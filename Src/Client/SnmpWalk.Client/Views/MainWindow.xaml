<controls:MetroWindow x:Class="SnmpWalk.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:SnmpWalk.Client.Views"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="400" 
        Width="550"
        MinHeight="400"
        MinWidth="550"
        ShowIconOnTitleBar="True"
        WindowStartupLocation="CenterScreen"
        GlowBrush="{DynamicResource AccentColorBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26" Name="MenuRow"/>
            <RowDefinition Height="30" Name="ToolBarRow"/>
            <RowDefinition Height="*" Name="ContentRow"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Menu IsMainMenu="True">
                <MenuItem Header="_File"/>
            </Menu>
        </Grid>
        <Grid Grid.Row="1">
            <ToolBar>
            </ToolBar>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid Margin="0,5,5,5" 
                      Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">




            </DataGrid>
        </Grid>
    </Grid>
</controls:MetroWindow>
